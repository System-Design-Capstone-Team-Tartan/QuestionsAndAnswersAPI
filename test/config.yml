config:
  target: "http://localhost:3001/qa"
  phases:
    - duration: 60
      arrivalRate: 5
      name: Warm up
    - duration: 120
      arrivalRate: 5
      rampTo: 50
      name: Rame up load
    - duration: 600
      arrivalRate: 50
      name: Sustained load

scenarios:
  - name: "Get current product's questions"
    flow:
      - get:
          url: "/questions/?product_id=1"